name: Pull Request

on: 
  workflow_dispatch:
  pull_request:
  
jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - run: echo "🏗️ Building..."
  
  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - run: echo "🚥 Testing..."
